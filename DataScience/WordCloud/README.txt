This program creates a "WordCloud" based on any subject queried on Wikipedia. 

Just download the code to a local folder, set the variable "" to the desired subject to be queried via the Wikipedia API, run the program and go to the folder where the code is downloaded to see the .png with the WordCloud generated

Run the below commands to install the required libraries to execute the program:

pip install matplotlib
pip install wikipedia
pip install wordcloud


